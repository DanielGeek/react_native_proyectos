import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';
import Formulario from './componentes/Formulario';

const App = () => {
  return (
    <>
      <Formulario />
    </>
  );
};

const styles = StyleSheet.create({});

export default App;
